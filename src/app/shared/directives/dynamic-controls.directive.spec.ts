import { DynamicControlsDirective } from './dynamic-controls.directive';

describe('DynamicControlsDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicControlsDirective();
    expect(directive).toBeTruthy();
  });
});
