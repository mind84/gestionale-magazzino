import { DisplayResponseDirective } from './display-response.directive';

describe('DisplayResponseDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayResponseDirective();
    expect(directive).toBeTruthy();
  });
});
