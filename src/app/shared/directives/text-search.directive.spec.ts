import { TextSearchDirective } from './text-search.directive';

describe('TextSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new TextSearchDirective();
    expect(directive).toBeTruthy();
  });
});
